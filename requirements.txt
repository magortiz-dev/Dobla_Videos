# Requisitos probados en Windows 11 con Python 3.10–3.11
# Nota: necesitas FFmpeg instalado en el sistema (no es un paquete pip).

streamlit>=1.32
yt-dlp>=2024.12.13
pytube>=15.0.0

# ASR (Whisper) + dependencias científicas
openai-whisper>=20231117
torch>=2.3.0
numpy>=1.26,<2.0
scipy>=1.11

# Audio utils
pydub==0.25.1
pyaudioop==0.2.1         # o, alternativamente, audioop-lts==0.2.1
imageio-ffmpeg==0.4.9    # para tener ffmpeg en PATH en Cloud

# Traducción Google (Cloud si hay credenciales; si no, público)
deep-translator>=1.11.4
google-cloud-translate>=2.0.1

# TTS Azure (opcional; si no hay credenciales, se usa Piper)
azure-cognitiveservices-speech>=1.37.0

# Mejora del “despegado” de palabras en inglés (opcional)
wordninja>=2.0.0

# Para el traductor de Google
googletrans==4.0.0rc1
