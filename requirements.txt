# --- UI ---
streamlit>=1.32,<1.53

# --- Descargas (estable) ---
yt-dlp==2024.12.13
gdown==5.2.0
requests>=2.31,<3
imageio-ffmpeg>=0.4.9   # ffmpeg/ffprobe portables (fallback)

# --- Audio/Video base ---
pydub==0.25.1            # en Py 3.11 usa stdlib 'audioop' (OK)

# --- ASR (Whisper CPU) ---
openai-whisper==20231117  # versión estable
torch==2.3.1              # wheel CPU compatible en Linux
numpy==1.26.4             # Whisper + SciPy en Py3.11
scipy==1.11.4

# --- Traducción ---
google-cloud-translate>=2,<4  # usa translate_v2
deep-translator>=1.11,<2      # fallback local

# --- TTS (Azure) ---
azure-cognitiveservices-speech==1.41.1
